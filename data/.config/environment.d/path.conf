# PATH para a sessão gráfica inteira (systemd user environment).
# Lido pelo Plasma/SDDM no login: cobre Dolphin "Run in Terminal", autostart, etc.
# Não depende de ~/.zshrc (que só vale para shells interativos).
PATH=$HOME/bin:$HOME/.local/bin:$HOME/.opencode/bin:${PATH}