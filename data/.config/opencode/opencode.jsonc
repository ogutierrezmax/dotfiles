{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [".cursor/rules/context-tags.mdc"],
  "plugin": ["opencode-rules", "./plugin/response-rule.ts"],
  "mcp": {
    "supabase":{
      "type":"remote",
      "url": "http://localhost:54321/mcp",
      "enabled": true
    }
  },
  "permission": {
    "bash": {
      // última regra que match vence — "*" deve ser o PRIMEIRO

      // --- allow ---
      "git add *": "allow",
      "git status *": "allow",
      "git diff *": "allow",
      "git log *": "allow",
      "git pull *": "allow",
      "ls *": "allow",
      "grep *": "allow",
      "cat *": "allow",
      "mkdir *": "allow",
      "touch *": "allow",
      "cp *": "allow",
      "mv *": "allow",

      // --- ask ---
      "sudo *": "ask",
      "git commit *": "ask",
      "git push *": "ask",
      "chmod *": "ask",
      "docker run *": "ask",
      "docker exec *": "ask",
      // --- deny ---
      // SECURITY: sem vírgula final aqui — o parser de config do OpenCode
      // rejeita trailing comma e o MCP inteiro deixa de carregar.
      "rm -rf *": "deny"
    },

    // --- allow ---
    "edit": "allow",
    "read": "allow",
    "glob": "allow",
    "grep": "allow",
    "webfetch": "allow",
    "todowrite": "allow",
    "question": "allow"
  }
}
